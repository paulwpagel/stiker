# spec = Gem::Specification.new do |spec|
#   spec.name = "rindlet"
#   spec.summary = "The messaging devices used by rinda"
#   spec.version = "2.3.3"
#   spec.author = "FLA Inc. and 8th Light"
#   spec.files = FileList["lib/**/*.rb", "spec/**/*.rb", "Rakefile"]
#   spec.test_files = ["spec"]
#   spec.homepage = "" if spec.respond_to?(:homepage)
# end
# 
# GEM_SPEC = spec
# DEPENDENT_SUBSYSTEMS = [ 'rapid_decision', 'underwriting_system', 'rinda_server', 'rapid_support', 'management' ]
# require File.expand_path(File.dirname(__FILE__) + "/dev_util/tasks/gem_tasks")
